# Discord bot token from the Developer Portal (Bot tab > Reset Token). Never share this.
BOT_TOKEN=

# Optional. Minutes between YouTube checks. Default 5.
POLL_INTERVAL_MINUTES=5

# The channels to watch and the messages to post live in channels.json (copy channels.example.json).
